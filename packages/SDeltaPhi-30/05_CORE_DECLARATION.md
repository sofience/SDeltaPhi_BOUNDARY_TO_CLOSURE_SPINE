# Core Declaration

Editability claimed is not editability real.

Formal openness is not operational re-openability.

A default is editable only when revision is exercised, rollback remains timely, exceptions can materially re-enter, and authority can actually modify the default path.
