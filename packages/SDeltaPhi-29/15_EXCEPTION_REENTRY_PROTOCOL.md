# Exception Re-entry Protocol

An exception channel is live only if non-default signals can re-enter decision pathways.

A valid exception re-entry path should preserve and route:

```text
anomaly
minority path
edge case
contrary evidence
public-interest context
local knowledge
appeal evidence
```

The exception must be able to change, delay, contest, reopen, or review the default.
