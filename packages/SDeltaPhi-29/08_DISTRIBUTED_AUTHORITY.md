# Distributed Authority

A default cannot remain editable if all revision rights collapse into a single center.

Distributed Authority separates:

```text
design authority
revision authority
appeal authority
exit authority
audit authority
rollback authority
exception review authority
```

If the same center defines the default, controls evidence, manages appeal, controls rollback, and defines exceptions, editability may become formal theater.
