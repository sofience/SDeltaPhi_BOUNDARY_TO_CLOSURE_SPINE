# Exception Channel

Low-cost convergence compresses exceptions.

Exception Channel preserves non-default signals before they disappear into silence.

Examples include:

```text
anomaly
minority path
contrary evidence
edge case
failure signal
local knowledge
public-interest context
```

Exception Channel is live only if exceptions can affect the transition path.
